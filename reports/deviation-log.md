# deviation log — 110_lockon

> Thresholds are never edited to make a gate pass. Any change to a threshold, gate semantics or
> plan clause is a dated row with the authority named. The >5x rule (§5) also lands here.

| # | date | what changed | from → to | why | authority | gate semantics changed? |
|---|---|---|---|---|---|---|
| 1 | 2026-09-04 | project.md WSL2 / .wslconfig references dropped | WSL2 exec + wslconfig check → native Windows, GLFW render | execution law §6 + ws/CLAUDE.md: no WSL; project.md v1 header predates the migration | lead (plan §9 D2) | **no** |
| 2 | 2026-09-04 | env render cross-check (SPEC test 8) | per-state IoU ≥ 0.7 → mean ≥ 0.7 AND per-state ≥ 0.5 + converse check (no box ⇒ no pixels) | the AABB projection is deliberately loose under perspective (one sample measured 0.695 with correct geometry); a transform bug drops samples far below 0.5 | lead (review finding 1) | **no** (instrument test, not a gate threshold) |
| 3 | 2026-09-04 | LockTracker primes the library with the first detection twice | — | `trackers` returns id −1 on the spawn frame unconditionally; priming = "activate on first frame" (official ByteTrack behaviour). Inflates retention ≈ +1 step/episode equally for every arm; comparisons unbiased | lead (review finding 5) | **no** |
| 4 | 2026-09-04 | camera pitch wording in env SPEC | "−25° about body x" → camera axes built from the intended end state (forward = (cos25°, 0, −sin25°)) | a rotation about the forward axis cannot pitch | impl, ratified by lead (review finding 15) | **no** |
| 5 | 2026-09-04 | track noise `occlusion_dropout` trigger | box height < 24 px → < 25 % of image height | 24 px never occurred in the arena (min 95 px): the dial term was inert | lead (review finding 2) | **no** (G3 thresholds unchanged; retest) |
| 6 | 2026-09-04 | G3 synthetic tests (tests/test_track.py) | single-seed retention ≥ 0.75 (occlusion) / ≥ 0.9 (lights-cut) → 20-seed rate: occlusion hold ≥ 70 % and mean ≥ 0.65; lights-cut hold 20/20 and mean ≥ 0.9 | after fixing the inert dropout term (row 5) the effective noise rose and seed 0 lost the id at re-emergence. Diagnosed: the lost track's Kalman velocity error under 4.4 px jitter drifts the predicted box over a 20-step coast to IoU 0.06 < 0.1; hold-rate is FLAT in box speed (2–8 px/step) and in state model (XYXY / XCYCSR / XCYCWH), all 16/20 at dial 0.3 — a structural ByteTrack property, not an instrument defect. Measured: occlusion 16/20 held, mean 0.710 (ceiling 0.833); lights-cut 20/20, mean 0.960 | lead | **no** (G3 = "id held through occlusion" is now asserted as the measured rate with margin; the retention numbers the owner reads come from the harness, unchanged) |
| 7 | 2026-09-04 | occlusion showcase scene seed | fixed seed 11 → first seed in 11..60 passing `occlusion_showcase_ok` (gap ≥ 5 steps, post-gap retention ≥ 0.8, same id) | at seed 11 the scripted gap is 30 steps = the tracker's coast buffer, the id dies and the shot shows a permanent loss. Shots are showcases and are SELECTED by a written rule, never tuned; the statistics live in the G6 curves. Rule + seed recorded in the scene module and the demo manifest | lead | **no** (no gate reads this scene's retention) |
| 8 | 2026-09-04 | RL reward "visible" term (project.md §3 "+visible/step") | state visibility → frozen-tracker lock held (tracker runs in the RL loop on state-derived noisy boxes, no pixels) | measured: PPO at 600k steps had the HIGHEST state visibility (0.83 vs scripted 0.78) and the LOWEST retention (0.10): box-centre displacement 8.5 px/step vs 2.5 static, 5.6 distinct ids/episode vs 1.2. Reward and headline metric had come apart; lock IS the metric | lead (D13) | **no** (G5 unchanged: PPO > static on retention) |
| 9 | 2026-09-04 | RL exploration + drone command response | ent_coef 0.01 → 0.0; log_std_init 0 (std 1.0) → −1.6 (std 0.2); drone velocity/yaw commands first-order filtered (tau 0.3 s, D14) | measured with the lock reward: the first random rollout scored −8/episode vs +100 for the static camera (std-1 actions saturate: camera thrashes, target leaves FOV within steps; PPO never samples a locked state). Filter → +40; std 0.37 still loses 38 % of steps out-of-FOV (retention 0.21 vs 0.44 static); std 0.2 keeps retention 0.39, std 0.1 → 0.42. Yaw compensation of detections measured FLAT (0.222 vs 0.223) and rejected | lead | **no** (G5 unchanged) |
| 10 | 2026-09-04 | plan §6 fork accounting | "3 × ≤5 h local runs" → local run counts 1 (run 4: PPO 0.545 vs scripted 0.547, tie); the HPC 45-unit array (seeds × rewards × arenas) is attempt 2, judged best-unit vs scripted on ≥ 80 held-out episodes at step 9; a third attempt only with the owner's +300 GPU-h. Hero = scripted (the §6 default) until then. Runs 1–3 were instrument-defective (fixed layouts; visibility reward; std-1 exploration) and are not counted | lead (review F10) | **no** (fork wording kept; plan §5 "local PPO stops once G5 passes" made it unfireable locally, this names where the remaining counts live) |
| 11 | 2026-09-04 | eval-time tracker noise seed | one frozen seed (EVAL_NOISE seed 0) for every episode → per-episode seed = episode seed (same distribution; training already did this) | all 20 gate episodes shared one noise realization, hiding the variance row 6 measured (review F4). G4/G5 re-run on seeds 1000–1019 after this row | lead | **no** |
| 12 | 2026-09-04 | policy observation "time since seen" + last-seen pose | updated on state visibility → updated on the frozen tracker's lock (the drone's own perception) | the reward pays for tracker lock (row 8) but the policy could not observe it (review F8): jitter, id switches and the 1-step latency were invisible to it. Pre-registered: PPO run 5 with this observation; if it does not beat scripted on seeds 1000–1019 it stays a tie under row 10 | lead (D15) | **no** (G5 unchanged) |
| 13 | 2026-09-05 | PPO run 5 (row 12 observation change) verdict | — | held-out seeds 1000–1019, per-episode noise: run 5 best 0.431 vs static 0.460 (G5 FAIL) vs scripted 0.562; run 4 checkpoint re-scored under the same path with its own state-visibility observation (sidecar `best.zip.obs.json`): 0.532 vs static 0.460 (G5 PASS), vs scripted 0.562 (does not beat). Design rejection → fork counter 2 of 3; canonical PPO artifact = run 4; hero = scripted | lead | **no** |
| 14 | 2026-09-05 | occlusion showcase selection rule (row 7) | gap ≥ 5 + post-gap retention ≥ 0.8 → gap 5–20 steps (< the 30-step coast buffer), id held again ≤ 5 steps after the gap, ≥ 0.8 retained over the next 20 steps; candidates 11–110; clip window derived from that gap | under per-episode noise the first passing seed (29) had a 30-step gap with reacquisition in the last frames: the clip showed a loss, not a recovery (visual check). The rule now says what "showcase" means; the shot is still selected, never tuned | lead | **no** (no gate reads this scene) |
| 15 | 2026-09-05 | PERUN environment build | offline cp311 wheelhouse (ws/100 pattern) → online `uv` venvs built on the login node (`scripts/hpc/hpc_bootstrap_online.sh`: CPU torch for the array, cu126 for the GPU trial) | measured: login02 reaches PyPI (HTTP 200), has `uv` 0.11 and `/usr/bin/python3.11`; the wheelhouse would have cost a multi-GB transfer over a 30 Mbps line for no change in what runs (>5x rule, refactored_method §5). Compute nodes stay offline; venvs live in the repo dir | lead | **no** |
| 16 | 2026-09-05 | HPC partitions/account (plan §7 placeholders) | `<FILL>` → cpu_short (array), gpu_short (prey trial + renders), account perun26011488 | owner approval 2026-09-05 ("partition per HPC_RUNBOOK"); `sinfo` lists cpu_short/cpu_long/gpu_short/gpu_long/viz | owner + lead | **no** |
| 17 | 2026-09-05 | CPU-array unit wall limit / cost estimate | est_h_per_unit 1.0 h (SLURM --time 01:30, timeout 5400 s) → 2.0 h (--time 03:00, timeout 10800 s); CPU-h estimate 45 → 90 | measured mid-flight on cn08: 3.2M of 6M steps in 53 min ⇒ ~1.7 h/unit, so every one of the 45 units would have been killed at 90 min with no result.json. First wave (82248) cancelled at 53 min and resubmitted as 82268 before the array burned CPU-h for nothing. The 1.0 h input came from an 8-core laptop; a budget input, not a gate threshold | lead | **no** (GPU-h ask unchanged at 22; CPU-h is unmetered, plan §7) |
| 18 | 2026-09-05 | learned-prey trial verdict (plan §7 GPU line) | — | 20M steps on one H200 (~2.3 GPU-h of the ≤20 h line). Against the learned prey: static 44.0→27.6 (Δ −16.4 [−25.0,−7.7]), scripted hunter 52.0→50.8 (Δ −1.2 [−11.8,+9.5]), local PPO hunter 51.3→35.9 (Δ −15.4 [−25.1,−5.6]); n=80 held-out seeds. The evader trained against the scripted hunter and could not exploit it, but its evasions transfer to policies it never saw. Strengthens the plan §6 default (scripted = hero) with a mechanism, not a tie | lead | **no** (no gate reads the prey trial) |
| 19 | 2026-09-05 | prey-trial claim as published in the README | "the scripted hunter is the only policy an adversarially trained evader cannot degrade" → the measured interaction (scripted gives up 14.2 pp less than PPO, paired CI [+5.1,+23.9]) plus three caveats: the evader plateaued after 2M of 20M steps and its checkpoint is a −1.9σ draw from that plateau; it never beat the hand-written prey against its own training opponent (53.2 vs 54.3); the scripted prey is capped at 69 % of the speed the learned prey may use | adversarial review 2026-09-05 found the original wording attributed a failed optimisation to the defender, and used per-arm CI overlap where only a paired interaction test is valid. Mechanism corrected too: FOV losses rise against static/PPO and FALL against scripted — the evader leaves the frame, it does not hide | lead (review F1, F2, F3, F5) | **no** (no gate reads the prey trial) |
| 20 | 2026-09-05 | learned-prey reward | pays on `state.person_visible` → pays on the frozen tracker's lock, mirroring row 8/D13 for the hunter | the trial optimised a proxy the project had already measured as decoupled from the reported metric; the 18M-step plateau is the likely consequence. The published run stands as reported (attempt 1, visibility reward); the replication seeds run the corrected objective and are labelled separately | lead (review F7) | **no** |
| 21 | 2026-09-05 | `PreyGym` tracker priming | primes at reset with a `t=0` update then steps at `t=1`; `run_episode` does neither | a one-step offset in the frozen hunter's perception between prey training and evaluation, ~1 step in 200, direction unknown. Recorded, not re-run for | lead (review F8) | **no** |
| 22 | 2026-09-05 | license/visual guard blob rule (G8) | blobs banned outside `tests/fixtures/` → plus a per-file `ALLOWED_TRACKED_BLOBS` allowlist, class `sim-checkpoint`, restricted to `models/` and ≤ 5 MB | the README's public numbers were computed from checkpoints no reader could obtain (review F6). The ban exists to keep dataset-derived content out; these policies are trained entirely inside this repo's simulator and contain none. Default-deny is preserved: two files are listed by hand, a new test asserts the class, the path prefix and the size | lead | **no** (G8 still fails on any unclassified blob) |
| 23 | 2026-09-05 | how a value reaches a PERUN job script | `sbatch --export=ALL,VAR=value` → the value is baked into a generated copy of the script, submitted plainly | measured: both GPU jobs submitted with `--export` were requeued **HELD** with reason "user env retrieval failed" and `scontrol release` did not clear them, while 17 GPU nodes sat idle. Resubmitted identically without `--export`: both started within 25 s. Every earlier job (smoke, 45-unit array, prey trial, probe) had used no `--export` — which is why this only surfaced at step 9 | lead | **no** |
| 24 | 2026-09-05 | plan §6 fork verdict — PPO vs the scripted hunter | attempt 3 (the 45-unit PERUN array) RESOLVED: no | pre-registered rule (row 10): best unit by selection-seed retention, judged on held-out seeds 1000–1079 at n=80. `k_short_d0.3_s3` scored 55.5 % on its own 20 selection seeds and **49.4 % held out** — below the scripted hunter (52.0 %) and below the local run-4 policy (51.3 %); it beats only the static floor (44.0 %). 10 of 45 units "beat scripted" on selection seeds; the margin did not survive. Counter now **3 of 3**: the fork has fired, and its pre-registered default stands — hero = scripted hunter, PPO reported honestly. Continuing per §8, not stopping | lead | **no** (the threshold was never touched; the verdict is the rule executing) |
| 25 | 2026-09-05 | learned-prey trial, corrected objective (row 20) — replication at 2 seeds | attempt 1's claim ("scripted hunter barely degraded, −1.2 pp") does not hold | with the evader paid for breaking the LOCK rather than for geometric invisibility, every camera policy is degraded: pooled over seeds 1–2 (n=160) static −20.2 pp [−26.4,−14.0], scripted −9.8 [−17.2,−2.4], PPO −22.5 [−29.0,−16.0]; all three CIs exclude zero. What replicates is the ORDERING — scripted gives up 12.7 pp less than PPO [+6.5,+18.9] and 10.5 less than static [+4.7,+16.3], same sign in all three independently trained evaders (+14.2 / +7.4 / +18.0). Seed variance is large and stated: the same objective cost the scripted hunter 17.3 pp (seed 1) and 2.3 pp (seed 2). README rewritten to the replicated form; attempt 1's row kept, not deleted | lead | **no** |

