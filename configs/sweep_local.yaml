# Step-8 local curve set (gate G6). One axis varies, the others sit at 0.5 (plan.md §9 D5).
name: sweep_local
n_episodes: 20
seed_base: 1000
values: [0.0, 0.25, 0.5, 0.75, 1.0]
axes: [occluder_density, darkness, prey_speed, prey_aggressiveness, channel_dropout]
policies:
  static: static
  scripted: scripted
  ppo: runs/ppo_local/best.zip   # skipped with a warning if the file does not exist
out_dir: reports/sweep
curves_dir: reports/curves
